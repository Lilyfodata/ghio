#!/usr/bin/python
# -*- coding: utf-8 -*-

import nltk
# bigrams(list_of_str)  将列表中的字符串相邻两两组成二元语法结构
# 用bigrams 把文本变成双词组的形式：[(word1, word2), (word2, word3), (word3, word4)……(wordn-1, wordn)]。再之后就可以用这些来计算
